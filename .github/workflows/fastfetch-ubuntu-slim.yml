name: Fastfetch Ubuntu Slim
on:
  workflow_dispatch:
    inputs:
      use-prerelease:
        default: false
        type: 'boolean'
        required: false

jobs:
  download-and-run:
    name: setup and run
    runs-on: ubuntu-slim
    steps:
      - name: Setup Fastfetch
        uses: ./.github/workflows/fastfetch-base/ubuntu-x86_64
        with:
          use-prerelease: ${{ github.event.inputs.use-prerelease }}
      - name: Run Fastfetch
        run: fastfetch -c all